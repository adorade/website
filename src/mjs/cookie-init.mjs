//
// Cookie-consent init
// -----------------------------------------------------------------------------

window.addEventListener('load', () => {
  window.cookieconsent.initialise({
    theme: 'classic',
    type: 'opt-out'
  })
})
